-- 소개글 플레이스홀더 잔존 매물 처리 (제시만 — 실행은 대표님 판단)
-- 실행: Supabase 콘솔 SQL Editor (프로젝트 edcqvmgqskeoegpqxlzy 확인 후)
-- 근거: docs/ORDER 긴급 버그 / 코드 가드는 src/lib/draftQuality.js
--
-- ※ 전수 스캔 결과 대상은 정확히 1건이다 (2026-07-20 실측).
--   id 2a4115ad-81ca-45f6-abc3-0bfef12f2dee / '왓더버거 원주일산점' / status=published
--   ai_draft.description, ai_draft.facility 에 "(주소)" "(업종)" 등 빈칸 문장.
-- ※ 지우지 않는다. 아래는 대표님이 고르실 선택지이며 어느 것도 자동 실행하지 않았다.

-- ────────────────────────────────────────────────────────────
-- 0) 먼저 확인 — 기대: 1행
-- ────────────────────────────────────────────────────────────
-- SELECT id, shop_name, status, created_at, updated_at,
--        ai_draft->>'description' AS description
-- FROM listings
-- WHERE ai_draft::text ~ '\((주소|업종|보증금|월세|권리금|전용면적|층수|해당 층수|상호|미입력)\)'
--    OR edited_texts::text ~ '\((주소|업종|보증금|월세|권리금|전용면적|층수|해당 층수|상호|미입력)\)';

-- ────────────────────────────────────────────────────────────
-- 선택지 A (권장) — 공개만 내리고 글은 보존
--   소유자가 앱에서 "모두가 새로 써드릴까요?"로 다시 만들면 정상 공개된다.
--   글을 남겨두므로 무엇이 잘못됐는지 나중에도 확인 가능.
-- ────────────────────────────────────────────────────────────
-- UPDATE listings SET status = 'hidden', updated_at = now()
-- WHERE id = '2a4115ad-81ca-45f6-abc3-0bfef12f2dee';

-- ────────────────────────────────────────────────────────────
-- 선택지 B — 빈칸 문장만 비우고 공개 유지
--   설명문 블록이 화면에서 사라지고 사실 정보(위치·임대조건)만 남는다.
--   E2는 description이 없으면 그 카드를 렌더하지 않으므로 표시상 안전.
-- ────────────────────────────────────────────────────────────
-- UPDATE listings
-- SET ai_draft = ai_draft - 'description' - 'facility',
--     updated_at = now()
-- WHERE id = '2a4115ad-81ca-45f6-abc3-0bfef12f2dee';

-- ────────────────────────────────────────────────────────────
-- 선택지 C — 아무것도 하지 않음
--   소유자가 다음에 수정·저장하려 하면 앱이 저장을 막고 다시 쓰라고 안내한다
--   (E1Step5 가드). 단 그때까지는 빈칸 문장이 계속 공개된다.
-- ────────────────────────────────────────────────────────────

-- ────────────────────────────────────────────────────────────
-- 재발 방지 (참고) — 앱 코드에서 이미 차단한다
--   · 생성 경로: E1Step2가 DB 로드 전에 생성하지 않도록 수정 (커밋 2cdb9a0)
--   · 저장 경로: findPlaceholderBlocks() 가 저장을 막음 (src/lib/draftQuality.js)
--   DB 레벨 CHECK 제약은 걸지 않았다 — ai_draft가 JSONB라 정규식 제약이
--   무거운 데다, 앱 밖에서 쓰는 경로가 없어 실익이 낮다고 판단.
--   필요하시면 아래 형태로 걸 수 있다 (실행 전 위 1건 정리 필수):
-- ALTER TABLE listings ADD CONSTRAINT listings_no_placeholder
--   CHECK (ai_draft::text !~ '\((주소|업종|보증금|월세|권리금|전용면적)\)');
