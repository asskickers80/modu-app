# ORDER-draft-block-enhance-v1 — 소개글 블록 3건 (브랜드 색·모두 수정 요청·사진 로드 버그)

## 모드: 자율 판정. 멈춤 (a)(b)(c)만.

## 1. [급함] 수정 모드 기존 사진 미로드
listingToLandlordContext image_urls 역매핑 여부 → 미매핑이면 수정 저장 시 빈 배열 덮임(손실) 확인. 손실 실재 시 역매핑 수정 + 덮인 매물 실DB 읽기 확인. E1 동일 점검. 테스트: 수정 진입→표시→무변경 저장→보존.

## 2. "모두" 브랜드 색
소개글 화면 "모두" 표기를 브랜드 블루(기존 규격 토큰)로 — DraftBlockCard 뱃지·안내 전수(E1·E1p 공용). 신규 색 금지.

## 3. 모두에게 수정 요청 (블록 단위)
[✏️직접 수정/전체 재생성]에 [모두에게 수정 요청] 추가 — 자유 텍스트 요청→해당 블록만 재작성→2안 비교(재생성 문법 재사용). Gemini 1회/요청, 그라운딩은 최초 생성만(블록 수정은 기존 본문+요청 — 비용·지연 절감, 근거 보고). 매물당 상한 자율(초과 시 정직 안내). DraftBlockCard 공용→E1 자동 획득. 요청·결과 학습 기록(기구현 재사용).

## 테스트: 사진 보존/색상/수정 요청→2안→반영·상한. 전체.
## 보고: 4줄(ls-remote) + 손실 여부·덮인 매물 + 상한 판정 + auth-loop-fix-v1 상태(착수 전이면 우선).
