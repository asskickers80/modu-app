# 세션 핸드오버 — 2026-07-08

> 작성: Claude Sonnet 4.6 | 마지막 커밋: `7c3c155`

---

## 완료

### 로그인 체계 1차 — 통합 구조 구축

| 항목 | 커밋 | 내용 |
|------|------|------|
| `lib/auth.js` 신설 | e88137c | `finishLogin()`: profiles 체크·생성·navigate 공통화. `migrateDeviceId()`: device_id 기반 listings·conversations를 auth user_id로 귀속 |
| A4 로그인 화면 재설계 | e88137c | 카카오(주 버튼) / 네이버('곧 지원' 배지, 더미 nav) / 이메일(가입·로그인·재설정 인라인 전환) / Google·Apple 구조 주석 준비 |
| 이메일 가입·로그인·재설정 | e88137c, 7c3c155 | signUp+signInWithPassword 분기. 로그인 우선 원칙(emailMode default='login'). Magic Link 계정 막힘 안내(amber 박스) + "비밀번호 재설정하기 →" 직접 버튼 |
| `AuthResetPasswordPage.jsx` 신설 | e88137c | PASSWORD_RECOVERY 이벤트 → 새 비밀번호 입력 → updateUser → 2.5초 후 /a4 |
| AuthCallbackPage 일반화 | e88137c | finishLogin 위임, "로그인 처리 중..." 일반화 |
| AuthKakaoCallbackPage 통합 | e88137c | finishLogin 위임, profiles에 `provider: 'kakao', kakao_id` 저장 |
| `/auth/reset-password` 라우트 | e88137c | App.jsx에 추가. `/auth/naver-callback` 자리 주석 예비 |
| conversations FK 설계 수정 | fe3c3ad | `user_id` 단일 → `sender_user_id + receiver_user_id` 분리 (단일 컬럼은 두 당사자가 덮어쓰는 구조적 결함) |
| DB 스키마 적용 | 대표님 직접 실행 | profiles CREATE + RLS 3정책. listings.user_id, conversations.sender/receiver_user_id ALTER 완료 |

### 카카오 로그인 KOE205 해결 + 이전 완료 사항

- `0b03cf6`: 카카오 커스텀 OAuth(scope 제거), 헤더 홈 버튼, bizLabel 버그, E1Step1 층수 드롭다운
- `2ee7158`: Playwright viewport 390px (일부), 수정버튼 좌표 단언
- `9a30bf4`: STATUS.md 갱신 핸드오버 (직전 세션)

---

## 로그인 잔여 — 서비스 오픈 준비 단계로 보류 (대표님 결정)

| 항목 | 상태 | 비고 |
|------|------|------|
| Supabase Auth 콘솔 설정 | 미실행 | Confirm email OFF + Redirect URL(`/auth/reset-password`) 등록 필요 — 대표님 직접 |
| 카카오·이메일 실 로그인 검수 | 미실행 | 실 브라우저 로그인 스크린샷 + profiles row 생성 확인 |
| 네이버 OAuth 구현 | 보류 | 방안 보고 완료(커스텀 OAuth 권장). 대표님이 네이버 개발자센터 앱 등록 후 착수 |
| conversations.sender/receiver_user_id | 코드 준비됨 | 콘솔 ALTER 실행 여부 — 대표님 직접 확인 필요 |
| device_id → user 마이그레이션 | 코드 준비됨 | `migrateDeviceId()` 로그인 후 자동 실행. 실 데이터 귀속은 실 로그인 검수 후 |
| Magic Link vs 이메일+비밀번호 | 결정 대기 | 대표님 의향: "임시 방편 싫다, 제대로 가고 싶다" → 다음 세션 첫 안건 |

---

## 다음 세션 1순위 (대표님 지정)

**A1~A4 진입 흐름 전면 재검토**
스플래시 → 카테고리 선택 → 온보딩 질문 → 로그인 전체 UX를 처음부터 다시 점검.
로그인 구조 결정(Magic Link 단일화 vs 이메일+비밀번호 유지)도 이 세션에서 확정.

---

## 후보 안건 (우선순위 순)

1. **계정 스위처** — 오더 문안 확정됨. 카테고리별 6인격 빠른 전환 UI
2. **시설·집기 빨간펜** — E1Step3 항목 리뷰
3. **Playwright viewport 390px 전역 고정** — playwright.config.js
4. **D4 설계 세션** → 실 상대방 수락 흐름

---

## 알아야 할 것들

### profiles 테이블 없어도 앱이 동작처럼 보인 이유
`maybeSingle()` 에러 시 `{ data: null }` 반환 → 항상 신규 분기 → insert 실패 무시 → localStorage category로 navigate. 실제로는 프로필 저장이 안 됐는데 작동처럼 보이는 구조. 현재는 CREATE TABLE + RLS 적용됨(대표님 실행 완료).

### Supabase 프로젝트 식별
URL: `edcqvmgqskeoegpqxlzy.supabase.co` — SQL 에디터에서 프로젝트가 여럿이면 반드시 이 URL로 확인.

### React Strict Mode 이중 실행 가드
`AuthKakaoCallbackPage.jsx`: sessionStorage `kakao_code_used` 플래그로 OAuth code 이중 소비 방어.

### 하드코딩 환경변수 (출시 전 필수)
`AuthKakaoCallbackPage.jsx` 안에 `KAKAO_REST_KEY`, `KAKAO_CLIENT_SECRET` 하드코딩됨 — `.env`로 이전 필요.

---

## 현재 테스트 현황

- Playwright: 145개 — 전체 PASS (외부 API 의존 0)
- 빌드: dev 서버 0 에러
