# 세션 핸드오버 — 2026-07-07~08

> 작성: Claude Sonnet 4.6  
> 마지막 커밋: `b8801e7`  
> 테스트: Playwright 144 passed / 0 failed  
> 다음 세션 첫 조각: E1 Step2 ✏️수정 버튼 실화면 미표시 원인 파악

---

## 이번 세션 완료 목록 (커밋 순)

### 오더 1 — A7 헤더 로고 확대 (`e8bf4fa`, `74780c5`)
- 워드마크 "모두" 한글 텍스트 제거
- 심볼 크기 26→34로 확대 (6개 대시보드 전부)
- 어르신 가독성 기준 결정

### 오더 2 — Gemini 폴백 모델 체인 (`9bd1471`)
- `src/lib/gemini.js` — `gemini-2.5-flash-preview` 실패 시 `gemini-2.0-flash` 자동 재시도
- **교훈**: 이전 세션 "완료 주장" 이 허위였음 — `git show 9bd1471`로 실증. 헌법 위반 사례.
- 검증: `gemini.js` 파일 위치와 폴백 로직은 반드시 `git show 9bd1471 -- src/lib/gemini.js` 로 확인

### 오더 3 — E1 AI 초안 흐름 단순화 (`2ee1d24`, `a48477e`)
- **2ee1d24**: 생성 완료 즉시 편집 — 단계 선택(그대로/수정/공개안함) 없이 바로 수정 가능
- **a48477e**: E1Step3(선택 관문) 완전 삭제
  - 컴포넌트 파일 삭제 + 라우트 제거 + 뒤로가기·다음버튼·딥링크 전부 재배선
  - **교훈**: "관문 제거 = 컴포넌트 삭제 + 라우트 + 모든 진입경로 재배선"

### 오더 4 — 상호 공개/비공개 옵션 (`d190954`)
- E1Step5 최종 화면에 상호명 공개 토글 추가
- 탐색 카드에서 비공개 상호는 "비공개"로 마스킹
- `shop_name_public` 컬럼(기존 존재) 활용

### 오더 5 — 시설·집기 매핑 현황 덤프 (코드 무변경, 조사만)
- `E1Step4.jsx` 시설·집기 카테고리 맵 구조 덤프
- 직접입력 항목 DOM 표시 버그는 이전 세션에서 이미 수정됨(`68f63d9`) 확인

### 추가 — E1 흐름 4단계 재번호 (`fbae2b6`)
대표님 요청: "아직도 관문이 남아 있다"

**Before**: `/e1/1` → `/e1/2` → (E1Step3 삭제돼 없음) → `/e1/4` → `/e1/5`  
**After**: `/e1/1` → `/e1/2` → `/e1/3` → `/e1/4`

- 프로그레스바 5칸→4칸 (1/4, 2/4, 3/4, 4/4)
- 시설·집기 "입력할게요/건너뜀" 관문 제거 (카테고리 직행 표시)
- `tests/e1-flow-4screens.spec.js` 신규: 4화면 단언 + 각 스크린샷 4장

### 추가 — E1 Step2 항목별 카드 UI + item_visibility (`3888829`)

대표님 지시: "화면은 늘리지 말고, 기능을 이 화면 안으로 흡수한다"

**설계**:
- `buildListingBlocks.js`: 각 블록에 `source: 'ai' | 'input'` 필드 추가
- `BlockCard` 컴포넌트: 항목 헤더에 AI배지(`AI 작성 ✦`), 공개/비공개 토글
- AI 작성 → 사용자 수정 시 배지 자동 제거
- `itemVisibility: {}` → E1Context에 추가
- `item_visibility JSONB` 저장(E1Step5) · 복원(listingToContext) · E2 비공개 체크(blockText)
- `salesAnalysis` 블록: 기본 비공개 (매출 정보 민감)

**SQL (대표님 콘솔 실행 완료)**:
```sql
ALTER TABLE listings ADD COLUMN IF NOT EXISTS item_visibility JSONB DEFAULT '{}';
```

### 추가 — E1 Step2 ✏️수정 버튼 (`b8801e7`) ← **미해결 버그 있음**

**구현 내용**:
- `BlockCard` 헤더 우측에 `✏️ 수정` 버튼 항상 표시 (`data-testid="edit-btn-{id}"`)
- 클릭 → 편집 모드: textarea 등장(`edit-textarea-{id}`) + autoFocus + 네이비 테두리
- `저장` 버튼(`save-btn-{id}`) 클릭 → textarea 사라짐, 수정 내용 표시, AI배지 제거
- Playwright `e1-step2-redesign.spec.js`: 144 통과

**미해결**:
- 실 브라우저에서 ✏️수정 버튼이 안 보인다는 대표님 피드백
- 하드 새로고침(`Ctrl+Shift+R`) 안내 완료, 미해결
- **다음 세션 첫 조각**: 원인 파악 (HMR 캐시? 빌드 오류? 다른 CSS 덮어씀?)

---

## 현재 코드 상태

### E1 흐름 4단계 구조
```
/e1/1  → E1Step1.jsx  — 기본 팩트 입력 (1/4)
/e1/2  → E1Step2.jsx  — AI 초안 항목별 카드 (2/4)  ← 버그 있음
/e1/3  → E1Step4.jsx  — 사진·증빙 (3/4)
/e1/4  → E1Step5.jsx  — 완성도 확인·공개 (4/4)
```
> `E1Step3.jsx`은 삭제됨. `App.jsx`에 `E1Step4`가 `/e1/3`에, `E1Step5`가 `/e1/4`에 매핑됨.

### item_visibility 동작 구조
```
E1Step2 BlockCard → setItemVisibility({ [blockId]: false })
                  ↓
E1Step2 다음 버튼 → update({ itemVisibility })  [E1Context]
                  ↓
E1Step5 saveListing → payload.item_visibility  [Supabase INSERT/UPDATE]
                  ↓
E2PropertyDetail blockText → visibility[key] === false ? null : ...
```

### 테스트 파일 목록 (관련)
| 파일 | 목적 |
|------|------|
| `tests/e1-flow-4screens.spec.js` | E1 4화면 단언 + 스크린샷 |
| `tests/e1-step2-redesign.spec.js` | ✏️수정 버튼 상호작용 |
| `tests/seller/listing.spec.js` | E1 전 단계 흐름 |
| `tests/seller-edge.spec.js` | 시나리오 1~8 |
| `tests/photo-split.spec.js` | 사진 내/외부 분리 |
| `tests/e1-edit.spec.js` | 수정 모드 진입 |

---

## 다음 세션 첫 작업

**E1 Step2 ✏️수정 버튼 실화면 미표시 원인 파악**

체크 순서:
1. 브라우저 개발자 도구 → Console 탭 오류 확인
2. `/e1/2` 로드 후 Elements 탭에서 `data-testid="edit-btn-description"` 존재 확인
3. 존재하면 → CSS 문제(z-index, overflow:hidden 등) 확인
4. 없으면 → HMR 미반영. `npm run dev` 재시작 후 재확인
5. 빌드 테스트: `npm run build && npm run preview` 후 동일 현상 확인

---

## 미결 의사결정 항목 (대표님 판단 필요)

| 번호 | 항목 |
|------|------|
| ① | 예시→실매물 승격 UX (수정 화면 "실매물로 공개" 버튼 vs 현행 재등록 안내) |
| ② | 룩스필라테스(코워크 실매물 92%) 노출 유지 여부 |
| ③ | E2L·임대인 축 — `docs/LANDLORD-PLAN.md` 7문항 |
| ④ | 동종 시장 동향을 네이버 뉴스 API 방식으로 채울지 |
| ⑤ | 가입/기기 식별 통합 방식 (로그인 도입 시 device_id 귀속) |
| ⑥ | AI 없이 등록된 매물(ai_draft null)의 완성도 처리 방침 |
| ⑦ | 폰 성능 — 빌드본(프리뷰 4173) 재측정 미완 |
