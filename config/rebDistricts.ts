/**
 * 행정동 코드 → 한국부동산원 상권명 수동 매핑 (ORDER 2026-09-11 파트 A1)
 * 비어 있어도 동작한다(시군구 값으로 폴백). 부동산원 기술문서에서 상권 단위 값이 확인되면 채운다.
 * // PLACEHOLDER — 대표 확정 전 (초기값: 서울 주요 상권 20개 내외 예정)
 */
export const REB_DISTRICTS: Record<string, string> = {
  // '1144066000': '홍대합정',   // 예시 — 확정 전이라 비워 둔다
}
