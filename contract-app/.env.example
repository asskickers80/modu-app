# Supabase 프로젝트 설정 (대시보드 > Settings > API에서 복사)
VITE_SUPABASE_URL=your_supabase_project_url
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key

# 인트라넷 중계(프록시) 모드 — '반영'(화면 캡처)을 쓰려면 필수
# 인트라넷 주소를 넣으면 개발 서버가 같은 출처로 중계한다.
# 이 모드에서 앱 접속 주소는 http://<PC IP>:5173/app/ 이 된다.
INTRANET_TARGET=http://192.168.0.10
